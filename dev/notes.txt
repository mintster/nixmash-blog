CREATING NEW BANNERS ----------------------------------------------------------- */

- Banner Size: 2048 x @720
- Save to ../nixmash/x/pics/home/banners/test.jpg
- Be sure to have created sites/nixmash links in build area: linkbBuild.sh --create
- Load http://localhost:9000/dev/banner?id=-1

- Mount /linode
- Copy images to server with: $ cpbanner.sh peaceful.jpg

Sql Insert:

INSERT INTO site_images (image_filename, image_description, image_author, source_url, common_license, banner_image, is_active)
VALUES ('peaceful.jpg', 'Peaceful', 'Nicholas Erwin', 'https://flic.kr/p/ZF9cLF', 1, 1, 1);

BUILDING FOR DEPLOYMENT ------------------------------------------------------- */

gradle mvc:bootRepackage

from .bashrc:
alias packageit='gradle mvc:clean mail:nixClean mvc:nixClean mvc:bootRepackage'

UPDATING PUBLIC BRANCH ON GITHUB ------------------------------------------- */

$ git-public (checkout public branch)
$ git-mrrb (see .bashrc for details)
$ git mergecheck
# if conflicts found:
$ git-meld-merge (in .bashrc) OR $ git conflicts (in .gitconfig)
$ git ac -m 'commit message...'
$ git push-public (see .gitconfig)

GRADLE BUILD ALL ---------------------------------------- */

project/root> $ install/sh/buildAll.sh


